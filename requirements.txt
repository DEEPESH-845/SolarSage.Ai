# SolarSage — runtime dependencies (web app)
# Agent research pipelines have their own extras in Agents/requirements.txt

# Web layer. The console is a separate Next.js app (see web/) and needs npm,
# not pip. Hardware/MQTT_OFFLINE/http_latencyTester.py wants Flask; install it
# separately if you run that tool.
fastapi==0.115.12
uvicorn==0.34.3

# Data layer
SQLAlchemy==2.0.41
pydantic==2.11.7
pydantic-settings==2.9.1

# Optional: enables the CrewAI agent orchestration path in Agents/crew.py.
# The pipeline runs standalone without it.
# crewai==0.130.0

# Computer vision (dust detection)
numpy==2.2.6
opencv-python-headless==4.11.0.86
